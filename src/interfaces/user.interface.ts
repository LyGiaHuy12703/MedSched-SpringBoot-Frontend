export type User = {
  id: string
  email: string
  name: string
  age: number
  gender: string
  dob: string
  address: string
  phone: string
  avatar: string
  active: boolean
}
export type RequestUpdateStatusUser = {
  status: boolean
}
